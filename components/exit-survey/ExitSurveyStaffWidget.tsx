// /components/exit-survey/ExitSurveyStaffWidget.tsx
//
// DEPRECATED — superseded by ExitSurveyStaffDashboard.tsx (the full
// /exit-survey page for PM/associate). This file called
// getRecentExitSurveysForStaff(), which no longer exists after the
// structured-AI/pod-context rewrite (see docs/EXIT_SURVEY_SYSTEM.md
// "Update log"). Kept only so a stale import fails loudly at compile time
// rather than silently breaking at runtime. Safe to delete once you've
// confirmed nothing still imports from here.
export {};